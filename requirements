# Windows Service & Process Monitoring Agent
# Core dependencies

psutil>=5.9.0

# Windows-only (required for live scanning on Windows)
# Comment these out if running on Linux/Mac in simulation mode
wmi>=1.5.1
pywin32>=306